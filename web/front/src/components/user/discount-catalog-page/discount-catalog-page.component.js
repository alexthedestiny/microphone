'use strict';

angular.
module('discountCatalogPage')
.component('discountCatalogPage', {
  template: require('./discount-catalog-page.template.html'),
  controller: ['$translate', '$scope', '$rootScope', '$filter', '$mdDialog', 'Main', 'UserService', '$stateParams', '$state',
  function discountCatalogPageController($translate, $scope, $rootScope, $filter, $mdDialog, Main, UserService, $stateParams, $state) {

    }
  ]
});
